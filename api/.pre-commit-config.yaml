# Pre-commit hooks for EchoTuner API
# Run manually: pre-commit run --all-files

repos:
  - repo: https://github.com/psf/black
    rev: 25.12.0
    hooks:
      - id: black
        args: ['--line-length=120']
        exclude: '^(migrations/|__pycache__/|.*\.pyc$)'
