import 'package:flutter/material.dart';

class AppColors {
    // Primary colors
    static const Color primaryLight = Color(0xFFA78BFA);
    static const Color primary = Color(0xFF8B5CF6);
    
    // Background colors
    static const Color surfaceVariant = Color(0xFF2A2A2A);
    static const Color surfaceBorder = Color(0xFF2A2635);
    static const Color background = Color(0xFF0F0A1A);
    static const Color surface = Color(0xFF1A1625);
    
    // Text colors
    static const Color textSecondary = Colors.white70;
    static const Color textTertiary = Colors.white54;
    static const Color textPrimary = Colors.white;
    
    // Status colors
    static const Color success = Color(0xFF4CAF50);
    static const Color error = Color(0xFFD32F2F);
    static const Color info = Color(0xFF666666);
    static const Color warning = Colors.orange;
    
    // Spotify specific
    static const Color spotify = Color(0xFF1DB954);
    
    // Progress indicator colors
    static const Color progressOrange = Colors.orange;
    static const Color progressGreen = Colors.green;
    static const Color progressBlue = Colors.blue;
    static const Color progressRed = Colors.red;
    
    // Status background colors for info messages
    static const Color successBackground = Color(0xFF0D4A1F);
    static const Color warningBackground = Color(0xFF4A2D0D);
    static const Color errorBackground = Color(0xFF4A0D0D);
    static const Color infoBackground = surface;
    
    // Status icon/text colors for info messages
    static const Color infoIcon = Color(0xFF8B5CF6);
    static const Color warningIcon = Colors.orange;
    static const Color successIcon = Colors.green;
    static const Color errorIcon = Colors.red;
    
    // Status text background colors (light variants)
    static const Color successTextBackground = Color(0xFFC8E6C9);
    static const Color warningTextBackground = Color(0xFFFFE0B2);
    static const Color errorTextBackground = Color(0xFFFFCDD2);
    static const Color infoTextBackground = Colors.white;
    
    // Utility colors
    static const Color transparent = Colors.transparent;
    static const Color grey = Colors.grey;
}
